<%= content_for :title, 'Top 10 unsustainable sites' %>

<%= content_for :head do %>
  <link rel="alternate"
        type="application/vnd.geo+json"
        href="<%= unsustainable_stats_path(format: :geojson) %>">
<% end %>

<div class="page-header"><h1>Statistics</h1></div>

<%= render partial: 'tabs', locals: { active: 'Unsustainable' } %>

<h2><%= content_for :title %></h2>

<p>
  These are the top 10 sites in Leeds that are earmarked for allocation.
  They have an IO/RAG of "G" (Green) and very low Sustainability Appraisal scores.
</p>

<div id="map" class="sites"></div>

<% if showing_scores? %>
  <%= render 'shared/marker_key' %>
<% end %>

<%= render partial: 'sites/sites_table', locals: { disable_sorting: true, numbered_rows: true } %>
